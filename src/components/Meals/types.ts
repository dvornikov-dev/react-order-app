export interface IMealModel {
  id: string;
  name: string;
  description: string;
  price: number;
}

export interface IMealItemProps {
  id: string;
  name: string;
  description: string;
  price: number;
}
